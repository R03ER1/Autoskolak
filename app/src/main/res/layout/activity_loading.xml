<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="24dp">

        <ImageView
            android:id="@+id/loadingIcon"
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:layout_marginBottom="24dp"
            android:src="@mipmap/ic_launcher_round"
            android:contentDescription="@string/loading_icon_description" />

        <TextView
            android:id="@+id/loadingStatusText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/loading_status_title"
            android:textColor="@color/text_primary"
            android:textSize="22sp"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginBottom="8dp" />

        <TextView
            android:id="@+id/loadingDetailText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/loading_detail_initial"
            android:textColor="@color/text_primary"
            android:textSize="14sp"
            android:alpha="0.8"
            android:gravity="center"
            android:layout_marginBottom="24dp" />

        <ProgressBar
            android:id="@+id/loadingProgressBar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="8dp"
            android:indeterminate="true"
            android:progress="0"
            android:max="100"
            android:progressTint="@color/correct_answer"
            android:indeterminateTint="@color/correct_answer"
            android:backgroundTint="@color/card_dark"
            android:layout_marginBottom="16dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/loadingRetryButton"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/loading_retry"
            android:textAllCaps="false"
            android:textColor="@color/text_primary"
            app:rippleColor="@color/button_pressed"
            app:backgroundTint="@android:color/transparent"
            android:visibility="gone" />

    </LinearLayout>

</FrameLayout>

