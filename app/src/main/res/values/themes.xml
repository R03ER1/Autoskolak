<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Autokolk.Base" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand colors -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        
        <!-- Secondary brand colors -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_dark</item>
        <item name="colorOnSecondary">@color/black</item>
        
        <!-- Status bar and navigation bar -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        
        <!-- Background colors -->
        <item name="android:windowBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/text_primary</item>
        
        <!-- Ripple/highlight color for taps (toolbar/menu/items) -->
        <item name="colorControlHighlight">@color/button_pressed</item>
        
        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        
        <!-- Button styles -->
        <item name="materialButtonStyle">@style/Widget.Autokolk.Button</item>
    </style>

    <style name="Theme.Autokolk" parent="Theme.Autokolk.Base" />

    <!-- Theme for MainActivity without action bar -->
    <style name="Theme.Autokolk.NoActionBar" parent="Theme.Autokolk">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Custom button style -->
    <style name="Widget.Autokolk.Button" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/button_normal</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="rippleColor">@color/button_pressed</item>
        <item name="cornerRadius">8dp</item>
        <item name="android:letterSpacing">0.025</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Answer button style -->
    <style name="Widget.Autokolk.Button.Answer" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/button_normal</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="rippleColor">@color/button_pressed</item>
        <item name="cornerRadius">8dp</item>
        <item name="android:letterSpacing">0.025</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:padding">16dp</item>
        <item name="android:gravity">start|center_vertical</item>
    </style>

    <!-- Theme overlay to neutralize green activation on SwitchMaterial -->
    <style name="ThemeOverlay.Autokolk.SwitchNeutral" parent="ThemeOverlay.MaterialComponents">
        <item name="colorControlActivated">@color/switch_track_off</item>
        <item name="colorSecondary">@color/switch_track_off</item>
    </style>
</resources>